"""Typed exception hierarchy.

Will hold ``CMCourierError`` (root), ``MetadataError``, ``MappingError``,
``AssemblyError``, ``UploadError``, etc. Populated in change
``002-domain-models-and-ports``.
"""
