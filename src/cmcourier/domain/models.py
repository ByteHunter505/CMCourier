"""Domain models.

Will hold ``TriggerRecord``, ``RVABREPDocument``, ``CMMapping``,
``ResolvedMetadata``, ``StagedFile``, ``MigrationRecord`` and the per-stage
status enum. Populated in change ``002-domain-models-and-ports``.
"""
