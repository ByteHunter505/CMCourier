"""Abstract interfaces (ports) implemented by adapters.

Will hold ``IDataSource``, ``ITrackingStore``, ``IAssembler``, ``IUploader`` and
any per-stage strategy interfaces (e.g., ``S0Strategy``). Populated in change
``002-domain-models-and-ports``.
"""
